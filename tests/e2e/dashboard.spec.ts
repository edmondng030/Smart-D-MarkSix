import{expect,test}from"@playwright/test";
test("dashboard presents verified live data",async({page})=>{await page.goto("/zh-HK");await expect(page.getByRole("heading",{name:"六合彩數據儀表板"})).toBeVisible();await expect(page.getByText("最近 10 期結果")).toBeVisible();await expect(page.getByText("熱門號碼 Top 5")).toBeVisible();await expect(page.getByText(/資料更新：/)).toBeVisible();});
test("mobile more menu exposes secondary tools",async({page,isMobile})=>{test.skip(!isMobile,"mobile navigation only");await page.goto("/zh-HK");await page.getByRole("button",{name:"更多"}).evaluate(element=>(element as HTMLButtonElement).click());await expect(page.getByRole("link",{name:"對獎工具"})).toBeVisible();await expect(page.getByRole("link",{name:"模型回測"})).toBeVisible();await expect(page.getByRole("link",{name:"管理員登入"})).toBeVisible();});
test("dashboard exposes gap heatmap and result sharing",async({page})=>{await page.goto("/zh-HK");await expect(page.getByText("最長未出現號碼")).toBeVisible();await expect(page.getByText("最近 10 期號碼區間熱度")).toBeVisible();await expect(page.getByRole("button",{name:"分享"})).toBeVisible();});